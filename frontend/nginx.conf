server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }
  location /api/ {
    proxy_pass http://api:5017;
    proxy_set_header Host $host;
  }
  location /health {
    proxy_pass http://api:5017;
  }
}
